/**
 * For more details on how to configure Wrangler, refer to:
 * https://developers.cloudflare.com/workers/wrangler/configuration/
 */
{
	"$schema": "../../node_modules/wrangler/config-schema.json",
	"name": "angular-movies-cf-worker",
	"main": "../../dist/projects/movies/cf-worker/server/server.mjs",
	"compatibility_date": "2026-06-17",
	"assets": {
		"binding": "ASSETS",
		"directory": "../../dist/projects/movies/cf-worker/browser"
	},
	"observability": {
		"enabled": true
	},
	"upload_source_maps": true,
	"compatibility_flags": [
		"nodejs_compat"
	],
	// SSR page cache. Create the namespace once with
	// `wrangler kv namespace create ssr_cache` and replace the id below.
	// Local `wrangler dev` uses a local KV store regardless of the id.
	"kv_namespaces": [
		{
			"binding": "ssr_cache",
			"id": "0000000000000000000000000000000000"
		}
	],
	"vars": {
		"NG_ALLOWED_HOSTS": "angular-movies-cf-worker.ng-movies.workers.dev",
		"NODE_VERSION": "24.15.0"
  	}
}
